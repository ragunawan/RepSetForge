# RepSetForge TODO

Prioritized backlog for features and polish beyond Phase 1 MVP (see `CLAUDE.md` → Acceptance Criteria). This is also the canonical home for scope from the original RPG-economy brief — gold, shop, onboarding, per-skill XP, quest scheduling, and more — that goes beyond Phase 1 MVP and isn't built yet.

## P0 - Finish And Verify Phase 1 MVP

- [x] Build and test the current app on the target simulator. (iPhone 16 unavailable in this environment; built/tested against iPhone 17 — build and all 25 tests pass.)
- [x] Complete the manual acceptance checklist (see `CLAUDE.md` → Acceptance Criteria). (Verified visually via screenshots: app name, pixel RPG theme, tab navigation, empty/seeded states. Items requiring live tap interaction were verified via `IntegrationTests.testQuestCompletionFlow()` instead — no UI-automation tooling, e.g. idb/XCUITest, is available in this environment. Recommend a manual pass in Xcode for the Reduce Motion and animation-feel checks specifically.)
- [x] Verify the end-to-end flow: create quest, add exercises, log sets, complete quest, award XP, unlock achievements, and show history. (Confirmed via passing `IntegrationTests.testQuestCompletionFlow()`.)
- [x] Confirm SwiftData first-launch seeding works on a clean install and with `--preview-data`. (Verified both: clean install shows Level 1/0 XP/no active quest with no crash; `--preview-data` seeds "Upper Body Strength" sample quest correctly.)
- [x] **Finish the manual chibi RPG art import.** All 407 required PNGs are now imported (`python3 scripts/import_rpg_art.py` reports 0 missing). Verified every `spriteAsset`/`rpg_bg_*`/`rpg_equip_*`/`rpg_skill_*` reference in code resolves to an imageset in `Assets.xcassets/RPG` (24 monsters, 5 bosses, 5 hero classes × idle/walk/attack/cast, 9 backgrounds, 8 equipment, 8 skills — all present), build succeeds with a clean asset-catalog link, and the Home scene renders the field background + knight sprite correctly in the simulator.

## P1 - Core Workout Tracking Improvements

- [x] Exercise templates: save common exercises with default muscle groups, notes, and set schemes. (Added `ExerciseTemplate` SwiftData model and `ExerciseTemplateService` for building an `Exercise` + prefilled `ExerciseSet`s from a template's default scheme, and vice versa. `AddExerciseSheet` (QuestDetailView.swift) gained a "Load Template" picker, a "Default Set Scheme" section with a "Save as Template" toggle, and a "Manage Templates" sheet for deleting saved templates. Covered by `ExerciseTemplateServiceTests`; build and all 29 tests pass. Not manually tap-tested in the simulator — no UI-automation tooling available in this environment, see other TODO notes.)
- [x] Quest templates: create reusable workout plans such as Push Day, Pull Day, Leg Day, and Core Trial. (Added `QuestTemplate` SwiftData model with self-contained `QuestExerciseBlueprint` snapshots (decoupled from `ExerciseTemplate` so deleting one never breaks the other) and `QuestTemplateService` for building a `Quest` from a template and vice versa. `QuestListView` gained a "New Quest" menu with Blank Quest / From Template / Manage Quest Templates, and `QuestDetailView` gained a "Save as Template" toolbar action. Covered by `QuestTemplateServiceTests`; build and all tests pass. Not manually tap-tested in the simulator — no UI-automation tooling available in this environment, see other TODO notes.)
- [x] Duplicate quest: start from a previous completed quest without re-entering every exercise. (Added `QuestDuplicationService.duplicate(_:)`, which clones a completed quest's exercises and each set's exact reps/weight (unlike `QuestTemplateService`, which only reapplies one default scheme) into a fresh active quest dated today with sets reset to uncompleted and no reward state. `QuestHistoryView` gained a "Duplicate" swipe action on completed quest rows that inserts the copy and navigates straight into `QuestDetailView` for editing. Covered by `QuestDuplicationServiceTests`; build and all 33 tests pass. Not manually tap-tested in the simulator — no UI-automation tooling available in this environment, see other TODO notes.)
- [x] Quest scheduling: allow creating a quest for today, a future date, or a past date (backdating a workout logged after the fact). (Added `QuestScheduler.status(for:)`, which derives `.planned` for a future calendar day and `.active` for today or earlier so backdated workouts can be logged and completed immediately. `QuestListView` gained a "Schedule Quest…" menu item opening `ScheduleQuestSheet` (name, date picker, optional "start from template" picker) alongside the existing instant Blank Quest / From Template actions. Covered by `QuestSchedulerTests`; build and all 37 tests pass. Not manually tap-tested in the simulator — no UI-automation tooling available in this environment, see other TODO notes.)
- [x] Edit or undo completed quests with full reward recalculation (XP, muscle levels, achievements) and no duplicate rewards — prefer rebuilding derived progression from completed-quest history over patching old totals in place. (Gold, skill XP, and PRs don't exist yet — see the RPG Economy and P1 Progression sections below — so this covers the progression state that currently exists. Added `ProgressionRebuildService.rebuild(context:)`, which resets character level/XP, muscle levels/XP, and achievement unlocks to baseline, then replays every `.completed` quest in chronological order through the existing `ProgressionService`/`AchievementService` calls, recomputing each quest's `totalXP` from its current exercises along the way. `AchievementService.checkAchievements` gained an `at:` parameter so replayed unlocks carry the original quest's completion date instead of `.now`. `QuestDetailView` gained an "Undo Completion" toolbar action (with a confirmation dialog) that reverts a completed quest to active and triggers a full rebuild — clearing its contribution without touching any other quest's rewards — so editing its exercises/sets and completing it again produces correct, non-duplicated totals. Covered by `ProgressionRebuildServiceTests` (idempotency, undo isolation, edited-set recalculation, achievement re-derivation); build and all 43 tests pass. Not manually tap-tested in the simulator — no UI-automation tooling available in this environment, see other TODO notes.)
- [x] Rest timer between sets, with configurable default durations per exercise; must not slow down set logging. (Added `defaultRestSeconds` (default 60) to `Exercise`, `ExerciseTemplate`, and `QuestExerciseBlueprint` — the last with a custom `Decodable` initializer that falls back to 60 for JSON saved before this field existed, since it's a Codable struct snapshot rather than a SwiftData attribute. `ExerciseTemplateService`, `QuestTemplateService`, and `QuestDuplicationService` all carry the value through their conversions. `ExerciseLoggingView` shows a non-blocking "Resting: Ns" banner (with a Skip button) driven by a plain `@State`/`Timer.publish` countdown that starts when a set is checked off complete; it doesn't gate or disable any other control, so adding sets, editing reps/weight, or checking off further sets keeps working while it counts down. The rest duration itself is editable per exercise (Stepper, 15s increments) and configurable as a default when adding a skill or saving/loading a skill template. Also fixed a pre-existing bug found in passing: `QuestTemplate` was never added to `PersistenceController.schema`, so the real app (not just tests, which each build their own ad-hoc schema) would have crashed the moment the Quest Board queried it — see the separate "Register QuestTemplate in the app's ModelContainer schema" commit. Covered by new/extended tests in `ExerciseTemplateServiceTests`, `QuestTemplateServiceTests` (including a legacy-JSON decode test), and `QuestDuplicationServiceTests`; build and all tests pass. Not manually tap-tested for the live countdown in the simulator — no UI-automation tooling available in this environment, see other TODO notes — but the app was launched and screenshotted to confirm the QuestTemplate schema fix didn't crash the Quest Board.)
- [x] Add support for bodyweight, assisted, distance, duration, and cardio exercise types. (Added an `ExerciseType` enum (`.strength`, `.bodyweight`, `.assisted`, `.distance`, `.duration`, `.cardio`) with `tracksReps`/`tracksWeight`/`tracksDistance`/`tracksDuration` flags driving which fields show. `Exercise` gained `exerciseType` (default `.strength`); `ExerciseSet` gained `distanceMiles` and `durationSeconds` fields alongside the existing reps/weight. `ExerciseTemplate` and `QuestExerciseBlueprint` carry matching defaults (`exerciseType`, `defaultDistanceMiles`, `defaultDurationSeconds`) so saved skill/quest templates round-trip the new type — `QuestExerciseBlueprint`'s custom `Decodable` init defaults all three for templates saved before this change. `ExerciseTemplateService`, `QuestTemplateService`, and `QuestDuplicationService` all thread the new fields through. The "Add Skill" sheet gained a Type picker that swaps the default-scheme fields shown (reps/weight, assist weight, distance, or duration) to match, and `ExerciseLoggingView`'s per-set row now shows only the fields relevant to the exercise's type. XP calculation is intentionally left as-is (reps/weight based) for this item — the next backlog line, "Add timed exercise XP formula," is where distance/duration-based XP gets designed, so cardio/duration/distance sets currently earn 0 XP until that's built. Covered by a new `ExerciseTypeTests` suite plus extended coverage in `ExerciseTemplateServiceTests`, `QuestTemplateServiceTests`, and `QuestDuplicationServiceTests`; build and all tests pass. Verified the app still launches and runs without crashing after the SwiftData model changes (screenshot-checked in the simulator); full tap-through of the new Type picker wasn't possible — no UI-automation tooling available in this environment, see other TODO notes.)
- [x] Add timed exercise XP formula for planks, runs, cycling, rowing, and circuits. (Added `ProgressionService.durationXP(seconds:)` (1 XP per 2 seconds under tension — planks, timed circuits) and `distanceXP(miles:)` (20 XP per mile — runs, rows, rides), plus a new `setXP(exercise:set:)` that dispatches by `exercise.exerciseType`: reps/weight for `.strength`/`.bodyweight`/`.assisted`, duration for `.duration`, distance for `.distance`, and both combined for `.cardio`. `exerciseXP(_:)` now calls this dispatcher instead of always assuming reps/weight, so distance/duration/cardio sets logged since the previous exercise-types change now actually earn XP through the existing `distributeXP`/quest-completion flow — no other call sites needed to change. Covered by new tests in `ProgressionServiceTests` (`testDurationXPIsOneXPPerTwoSeconds`, `testDistanceXPIsTwentyXPPerMile`, `testSetXPDispatchesByExerciseType`, `testExerciseXPUsesDurationFormulaForTimedExercises`); build and all tests pass.)
- [x] Add personal records for max weight, max reps, best volume, longest duration, and fastest pace (also the basis for PR bonus gold once the economy below exists — gold itself isn't built yet, see the RPG Economy section below). (Added a `PersonalRecordType` enum and `PersonalRecord` model (one per exercise name + record type, matched case-insensitively) and `PersonalRecordService.evaluateRecords(for:context:)`, which only proposes the record types that make sense per `ExerciseType`: `.strength` → maxWeight/maxReps/bestVolume, `.bodyweight`/`.assisted` → maxReps, `.duration` → longestDuration, `.cardio` → fastestPace (needs both distance and duration, which only `.cardio` tracks — plain `.distance` sets don't yield a record yet since "longest distance" isn't one of the five requested types). Wired into `QuestDetailView.completeQuest()`, surfaced as a "New Personal Records!" section on the completion screen, and browsable as a "Personal Records" list on the Character screen. For consistency with the existing "no duplicate rewards, rebuild from history" architecture, `PersonalRecordService.rebuildAll(context:)` recomputes every record from scratch by replaying completed quests and is now called from inside `ProgressionRebuildService.rebuild(context:)`, so undoing a completed quest correctly un-sets any PR that quest alone had set. Covered by a new `PersonalRecordServiceTests` suite (per-type gating, only-improvements-count, rebuild correctness/idempotency); build and all tests pass. Verified the app still launches without crashing on the Character tab after the new SwiftData model (screenshot-checked); full tap-through of the completion screen's new PR section wasn't possible — no UI-automation tooling available in this environment, see other TODO notes.)
- [x] Weight units: support pounds and kilograms on `ExerciseSet`, selected during onboarding, applied consistently through logging and history; keep unit formatting isolated so kg support doesn't confuse historical entries. (There's no onboarding flow yet — see the P1 Onboarding section below — so the unit choice lives in a new gear-icon Settings sheet on the Character screen instead; it'll move into onboarding once that flow exists. Added `WeightUnit` (lb/kg) and gave each `ExerciseSet` its own `weightUnit` — defaulting existing/new data to pounds — rather than a single global unit, so switching the preference later never reinterprets a weight that's already logged; historical sets always keep displaying in the unit they were actually recorded in. `PlayerCharacter.preferredWeightUnit` drives the unit new sets are tagged with (in `ExerciseLoggingView.addSet()`, `AddExerciseSheet`, and quest/template creation flows in `QuestListView`). `ProgressionService.setXP` and `PersonalRecordService`'s comparisons normalize to a common unit first, so XP and "is this a new PR" logic are correct regardless of which unit a lift was logged in; a weight-based `PersonalRecord` stores and displays in whichever unit actually set the record, not a fixed one. `PersonalRecordType.formattedValue` and all weight displays now take the relevant unit instead of hardcoding "lb". Along the way, found and fixed a real SwiftUI rendering bug: the new Settings gear button was invisible under `.navigationTitle`'s default large-title mode when paired with this screen's `ScrollView` + `ignoresSafeArea()` background — confirmed via screenshot, fixed by switching `CharacterProgressView` to `.navigationBarTitleDisplayMode(.inline)`, re-confirmed working via screenshot. Covered by new `WeightUnitTests` plus extended `ProgressionServiceTests` and `PersonalRecordServiceTests` (cross-unit comparison correctness); build and all tests pass.)

- [x] Autocomplete exercise names in the Add Skill sheet as the user types, suggesting from previously logged exercise names, to reduce typos and keep naming consistent across quests. (Added `ExerciseNameSuggestionService.suggestions(matching:exerciseNames:limit:)`, pure and stateless: case-insensitive substring match, prefix matches ranked above mid-string matches, then by how often that name has actually been logged, then alphabetically; deduplicates case-insensitive variants (keeping the first-seen casing) and excludes a name that already exactly matches the current query, since there'd be nothing left to autocomplete. `AddExerciseSheet` (in `QuestDetailView.swift`) gained a `@Query private var allExercises: [Exercise]` and a horizontal row of tappable suggestion chips beneath the Name field, shown only while there are matches; tapping a chip fills the name field. This is a separate, complementary mechanism from the existing `ExerciseTemplate` picker — templates suggest a *name + full default set scheme*, this suggests *just names* purely from logging history, with no template needing to be saved first. Covered by a new `ExerciseNameSuggestionServiceTests` suite (empty query/history, case-insensitive substring matching, exact-match exclusion, case-insensitive dedup, prefix-over-mid-string ranking, frequency ranking among equal prefix matches, limit enforcement, blank-name filtering); build and all tests pass. Verified the app still runs without crashing after the new `@Query` (screenshot-checked); the autocomplete chips themselves live several taps deep in `AddExerciseSheet` (Quest Detail → Add Skill), which this environment can't drive interactively — verified via the unit test suite instead.)
- [x] Per-exercise metrics screen: view max weight, best volume, and history/trend for a given exercise name, reachable from the Add Skill autocomplete or exercise history. (Added `ExerciseMetricsService.metrics(for:in:)`, purely derived from completed-quest history (case/whitespace-insensitive name matching, same rule `PersonalRecordService` uses) — unlike `PersonalRecord`, which only tracks the single best-ever value, this keeps the *full* chronological session-by-session trend (one point per quest, combining multiple same-named exercises within one quest), each with that session's max weight and volume normalized to pounds. Added `ExerciseMetricsView`: all-time max weight/best volume stat panels plus a Swift Charts line+point chart of max weight over time, with an empty state for names with no completed history. Reachable both ways the TODO asked: `ExerciseLoggingView` gained a "History" toolbar button opening it for the current exercise, and each autocomplete suggestion chip in `AddExerciseSheet` (from the previous item) gained an adjacent chart-icon button opening it for that name — letting the user preview an exercise's history before deciding whether to add it. Covered by a new `ExerciseMetricsServiceTests` suite (blank name/never-logged → nil, case/whitespace-insensitive matching, all-time max/volume correctness, chronological ordering, kg→lb normalization, incomplete-set/incomplete-quest exclusion, combining multiple same-name occurrences within one quest); build and all tests pass. Verified the app still runs without crashing after the new view/toolbar/sheet wiring (screenshot-checked); the screen itself sits multiple taps deep (Quest Detail → exercise → History, or Add Skill → suggestion chip → chart icon), which this environment can't drive interactively — verified via the unit test suite instead. This completes the "P1 - Core Workout Tracking Improvements" section.)

## P1 - Onboarding And RPG Economy

Full-economy scope from the original RPG brief: gold, ownable/purchasable equipment, and skills that level from real training instead of just character level. `RPGSkill` and `RPGEquipment` already exist as static, level/class-gated flavor data (`RepSetForge/Models/RPGSkill.swift`, `RPGEquipment.swift`) — extend them rather than replacing them.

- [x] First-run onboarding flow: introduce RepSetForge's concept, seed default player/muscle/skill/achievement/equipment state exactly once, and let the user choose pounds or kilograms. (Player/muscle/achievement state was already seeded exactly once by `PersistenceController.seedCoreDataIfNeeded()`; `RPGSkill`/`RPGEquipment` are still static, class-gated flavor data with no owned/persisted state yet — that's the next two TODO items below — so "seeding" them today means picking the hero class that gates which are usable. Added `OnboardingView`: a welcome screen explaining the quest/XP/muscle-leveling concept, a class picker (writes to the previously-unused `RPGEncounterState.rpgClass` hook that was already designed for exactly this), and a weight-unit picker (writes to `PlayerCharacter.preferredWeightUnit`, from the units feature above). Gated by a new `PlayerCharacter.hasCompletedOnboarding` flag, shown via a `.fullScreenCover` in `ContentView` with `.interactiveDismissDisabled()` so it can't be swiped away before finishing. Verified end-to-end via simulator screenshot: shows correctly on first launch with the class list and unit toggle both interactive-looking and Knight pre-selected; confirmed the underlying assignment logic separately since taps aren't available in this environment. Build and all tests pass.)
- [x] Add `gold` to `PlayerCharacter` and award it deterministically: small amount per completed set, larger amount per completed quest, bonus on PRs (e.g. totalXP / 10 gold per quest, +25 gold per PR). (Added `GoldService` with the exact formulas: 1 gold per completed set, `totalXP / 10` per quest, +25 gold per newly set/broken personal record — all pure functions of already-known counts, so replaying history always reproduces the same total. Wired into `QuestDetailView.completeQuest()` and shown as a reward row on the completion screen; `PlayerCharacter.gold` also shown on the Character screen. For the same "no duplicate rewards, rebuild from history" reasons as XP/achievements/PRs, `ProgressionRebuildService.rebuild(context:)` now resets gold to 0 and re-awards it per completed quest during replay — this also meant inlining `PersonalRecordService.evaluateRecords` directly into that replay loop (instead of calling the separate `PersonalRecordService.rebuildAll` afterward) so each quest's own newly-set-record count is available for its gold bonus; `PersonalRecordService.rebuildAll` is unchanged and still available/tested standalone. Not spendable yet — that's the Equipment/Shop screen, still below. Covered by new `GoldServiceTests` plus extended `ProgressionRebuildServiceTests` (formula match, idempotency, undo removes only its own contribution); build and all tests pass. Confirmed the Character screen's Gold row is correctly structured and unaffected by this change; couldn't screenshot it live since the onboarding flow (previous item) now gates first launch and there's no tap automation in this environment to complete it — first ran into a stale-binary mixup (an old DerivedData folder from before onboarding/gold existed) that made the feature look broken in a screenshot, traced it to two DerivedData directories and confirmed the real build was correct; removed the stale one.)
- [x] Turn `RPGEquipment` into an owned/persisted model (owned + equipped booleans, purchase source) instead of pure static data. (`RPGEquipment` itself stays static catalog data per the note above; added a new `OwnedEquipment` SwiftData model — `equipmentID` + `owned`/`equipped` booleans + `purchaseSource` + `acquiredDate` — as the per-player ownership layer on top of it. Added `RPGEquipmentService` with `seedStarterGear(for:context:)` (idempotent — grants the chosen class's level-1 weapon plus the universal "Beginner Armor", owned and equipped; called from `OnboardingView.completeOnboarding()` right after class selection, since starter gear needs to know the class), `equippedLoadout(context:)`, and `equip(_:context:)` (swaps the equipped flag within a slot; no-ops for unowned items). Previously equipment was **not** persisted at all — `RPGEncounterViewModel` auto-equipped "the best item the class/level allows" from the static catalog on every `configure()` call, so gear wasn't actually owned or earned. Changed `RPGEncounterViewModel.configure(snapshot:state:equippedLoadout:)` to take the resolved loadout from the caller instead of computing it itself; `RPGSceneView` now queries `OwnedEquipment` and resolves it via `RPGEquipmentService.equippedLoadout(from:)`. No shop/buy UI yet — that and equipment drops are the next two items below. Covered by a new `RPGEquipmentServiceTests` suite (per-class starter seeding, idempotency, slot-swap-on-equip, no-op on unowned); build and all tests pass. Verified the app still runs without crashing after the new SwiftData model and `RPGEncounterViewModel` signature change (screenshot-checked); couldn't verify the starter-seeding path fires end-to-end in the simulator since it requires completing the onboarding flow (previous item) and there's no tap automation in this environment — verified via unit tests instead.)
- [x] Build an Equipment/Shop screen: browse purchasable items, buy with gold, equip/unequip per slot, insufficient-gold and already-owned states, level/rarity gating. (Added `price: Int` and a computed `rarity: RPGItemRarity` (common/uncommon/rare/epic, derived from `requiredLevel` — no new per-item authoring needed) to `RPGEquipment`. Added `RPGEquipmentService.purchase(_:context:)` returning a `PurchaseResult` (`.success`, `.alreadyOwned`, `.insufficientGold`, `.levelLocked`, `.notFound`) that gates by class+level via the existing `isUsable(by:atLevel:)` before checking ownership and gold, and only deducts/records on success. New `EquipmentShopView` lists the catalog grouped by slot with a state-driven action per row (Equipped / Equip / Locked / price-in-red-if-unaffordable / Buy button) and a gold + level/class header; opened from a new "Shop" toolbar button on the Character screen for now, since the dedicated Gear/Shop tab is explicitly the next backlog item. Covered by new tests in `RPGEquipmentServiceTests` (purchase success/insufficient-gold/already-owned/level-locked/wrong-class, rarity derivation); build and all tests pass. Verified the app still runs without crashing after the schema/view changes (screenshot-checked); couldn't tap through an actual purchase in the simulator — no UI-automation tooling available in this environment, see other TODO notes — verified via unit tests instead.)
- [x] Turn `RPGSkill` progression into real XP: primary muscle-group XP grants 100% related skill XP, secondary muscle-group XP grants 40%, PRs grant skill XP bonuses — instead of skills unlocking purely from character level. (`RPGSkill` gained a `relatedMuscles: Set<MuscleGroup>` field (each catalog skill authored with 2 flavor-appropriate muscles) and a computed `unlockThresholdXP` (`requiredLevel * 100`, reusing the same difficulty knob rather than adding a second authored number). New `SkillProgress` model (one row per catalog skill, seeded idempotently alongside the existing muscle/achievement seeding) tracks real accumulated XP and an `unlocked` flag. `SkillProgressionService.distributeSkillXP(exercises:prExerciseNames:context:)` mirrors `ProgressionService.distributeXP`'s primary/secondary split but per-skill instead of per-character — 100% of an exercise's XP to skills related to its primary muscle, 40% to skills related to each secondary muscle, plus a flat +50 bonus to primary-muscle-related skills for any exercise that set a personal record this quest — and flips `unlocked` (with the historically-correct date) the moment accumulated XP crosses the threshold. Wired into `QuestDetailView.completeQuest()` and into `ProgressionRebuildService.rebuild(context:)` (resets all `SkillProgress` to baseline and replays it per quest, consistent with how XP/gold/PRs/achievements already rebuild from history). Passive-combat skill selection (`RPGEncounterViewModel.pickSkill`) now gates by `unlockedSkillIDs` when the caller provides one (`RPGSceneView` resolves it from persisted `SkillProgress` via `SkillProgressionService.unlockedSkillIDs(from:)`); `nil` falls back to the old level-only gating so untouched previews/tests keep their existing behavior. Covered by a new `SkillProgressionServiceTests` suite (seeding, primary/secondary split, PR bonus, unlock-on-threshold, unrelated-muscle no-op); build and all tests pass. Verified the app still runs without crashing after the new SwiftData model and `RPGEncounterViewModel`/`RPGSceneView` signature changes (screenshot-checked); couldn't verify an actual skill unlock end-to-end in the simulator — no UI-automation tooling available in this environment, see other TODO notes — verified via unit tests instead.)
- [x] Deterministic, occasional equipment drops from completed quests / PR milestones (e.g. every 3 completed quests) — must be idempotent on rebuild/replay, not random per-tap. (Added `EquipmentDropService`, which grants a drop on every 3rd completed quest and every 3rd cumulative personal record (`PlayerCharacter.totalPRCount`, a new field — counts PR *events* including re-breaking an existing record, not distinct `PersonalRecord` rows). The item is picked via a stable FNV-1a hash of a milestone-derived seed string (e.g. `"quest-3"`) modulo the class-usable, not-yet-owned candidate list — deliberately **not** `String.hashValue`, which is randomized per process launch and would have picked a different item on every app relaunch, silently breaking the "idempotent on rebuild/replay" requirement (caught and fixed before landing). Wired into `QuestDetailView.completeQuest()` (drops surfaced in a new "Equipment Found!" section on the completion screen) and into `ProgressionRebuildService.rebuild(context:)`, which now also resets `totalPRCount` and deletes only `"quest_drop"`/`"pr_drop"`-sourced `OwnedEquipment` rows before replaying (starter gear and shop purchases are untouched, since those aren't derived from quest history). Covered by a new `EquipmentDropServiceTests` suite (milestone gating, class-only candidates, no duplicate grants, exhaustion, and same-seed determinism across two independent `ModelContainer`s simulating separate app sessions); build and all tests pass. Verified the app still runs without crashing after the schema/view changes (screenshot-checked); couldn't trigger an actual milestone drop live in the simulator — no UI-automation tooling available in this environment, see other TODO notes — verified via unit tests instead.)
- [x] Add a Gear/Shop tab to `ContentView` once the shop screen exists; update the `--tab` launch-argument indices and their documentation in `CLAUDE.md` / `README.md` accordingly. (Added `EquipmentShopView` as a "Gear" tab between Character and History: 0=Quest Board, 1=Character, 2=Gear, 3=History, 4=Achievements — History and Achievements each shift by one index. Removed the temporary "Shop" toolbar button/sheet from `CharacterProgressView` now that the dedicated tab is the real entry point (and dropped `EquipmentShopView`'s now-unneeded sheet-style "Done" button/`dismiss` — it's a persistent tab now, not a modal). Updated the `--tab` documentation and index table in both `CLAUDE.md` and `README.md`, plus `README.md`'s view-file listing. Build and all tests pass. Verified live in the simulator: launched with `--tab 2`, confirmed the Gear tab renders correctly with the seeded Mage's equipped starter gear (Focus Staff, Beginner Armor) shown as "Equipped," locked items showing their level requirement, and correct rarity badges — this also incidentally end-to-end-verified the onboarding → starter-gear-seeding → Gear-tab-display pipeline all wired together correctly on a real device state, not just via unit tests.)
- [x] Equipped skill loadout: let the user choose which unlocked skill per category (attack/defense/magic) drives passive battles, instead of always auto-selecting the highest-level one. (Added `RPGSkillCategory` (attack/defense/magic) and assigned one to every catalog skill; `SkillProgress` gained an `equipped: Bool`, mirroring `OwnedEquipment`. `SkillProgressionService.equip(_:context:)` equips an unlocked skill and un-equips whatever else was equipped in the same category (exactly like `RPGEquipmentService.equip`); the first skill unlocked in an empty category auto-equips so combat isn't silent before the player visits the Gear tab, but never overrides an existing choice. `RPGEncounterViewModel.pickSkill` now gates by `equippedSkillIDs` (renamed from `unlockedSkillIDs` — passive combat now only draws from what's actually equipped, not everything unlocked); `nil` still falls back to the old level-only gating for untouched previews/tests. `ProgressionRebuildService.rebuild(context:)` deliberately does **not** reset `equipped` alongside `unlocked`/XP (unlike the equipment drop reset) — a skill choice is the player's own and should survive an unrelated quest's rebuild — but does clear the flag afterward for any skill that no longer re-unlocked during replay, so an orphaned flag never reports a locked skill as active. Added a "Skills" section to `EquipmentShopView` (grouped by category, showing XP-to-unlock progress for locked skills and an Equip button for unlocked-but-not-equipped ones) alongside the existing Equipment section. Covered by new tests in `SkillProgressionServiceTests` (auto-equip-first, auto-equip-doesn't-override, category-scoped swap, locked-skill no-op, unlocked+equipped guard) and `ProgressionRebuildServiceTests` (equipped choice survives an unrelated rebuild, orphaned flag clears when the unlock is undone); build and all tests pass. Verified the app still runs without crashing on the Gear tab after the schema/view changes (screenshot-checked).)

## P1 - Progression And RPG Systems

- [x] Level-up summary that clearly lists character and muscle group level changes. (The underlying data already existed — `ProgressionService.DistributionResult.characterLevelUp`/`muscleLevelUps` — but the completion screen only surfaced it as a small inline "LEVEL UP!" tag on each XP reward row, with no actual level numbers shown. Added a dedicated "Level Up!" section to `QuestCompletionView`, placed right after the header (before the XP breakdown, since it's the most exciting result) — one row per level change in a stable order (character first, then muscle groups), each showing "Lv `<old>` → Lv `<new>`" explicitly, correctly handling multi-level jumps from a single quest's XP overflow since it just displays whatever old/new values `LevelUpResult` already carries. Kept the existing inline tags too. No new files/models — pure UI addition reading already-tested distribution data (`ProgressionServiceTests` already covers the underlying level-up math), consistent with this codebase's convention of not adding dedicated tests for View-layer code. Build and all tests pass; verified the app still launches without crashing after the change (screenshot-checked) — couldn't tap through an actual quest completion to see the section live, same limitation as the adjacent Achievements/PR/Equipment-drop sections in this same file.)
- [x] Better achievement coverage for streaks, volume, balanced training, first PR, and consistency milestones. (Added 6 new achievements alongside the existing 7: `first_pr` ("First PR" — unlocks on the first ever `PersonalRecord`, via `context.fetchCount`), `volume_10k` ("Volume Crusher" — 10,000 lb of cumulative completed-set volume, reps × weight normalized through each set's own `WeightUnit` to pounds so kg-logged sets count correctly), `balanced_trainer` ("Balanced Trainer" — every muscle group has nonzero `totalXP`, i.e., actually trained at least once, cardio included), `three_day_streak` / `thirty_day_streak` (lower/higher bookends around the existing 7-day streak), and `ten_workout_days` ("Creature of Habit" — 10 *distinct* calendar days regardless of consecutiveness, a genuinely different "consistency" metric from streaks since it rewards showing up over time even with gaps). Generalized the old hardcoded `hasSevenDayStreak` into a reusable `hasStreak(ofDays:completedDays:)` shared by all three streak checks. Covered by 7 new tests in `AchievementServiceTests` (each new unlock condition, its negative case, and streak-threshold boundaries); build and all tests pass. Verified live in the simulator: launched into the Achievements tab and confirmed all 6 new achievements are seeded correctly with their descriptions, sitting alongside the pre-existing ones.)
- [x] Streak protection rules so rest days and late-night workouts behave predictably. (Base consecutive-day streak calculation already exists in `RPGProgressionSnapshot.streak` — this item is about edge cases, not the base feature.) (Audited the existing implementation against every edge case the item names and found it was already correct: it buckets completions by `calendar.startOfDay` and walks backward via `calendar.date(byAdding: .day, ...)` — calendar-*component* arithmetic, not raw 86,400s interval math — so DST's 23/25-hour days, arbitrary time zones, and late-night boundaries were never actually at risk. This matters beyond cosmetics since `currentWorkoutStreak` gates real gameplay (`RPGMilestoneQuest.workoutStreakDays`, boss unlocks). Rather than change working code, added 6 explicit regression tests to `RPGBossMilestoneTests` locking in: streak resets (not just pauses) once a full untrained day passes, duplicate same-day completions don't inflate the count, a workout just before midnight and another just after count as two consecutive days, a streak spanning a real DST transition (US spring-forward, March 2024) stays correct, and the function is deterministic under an arbitrary explicit time zone (UTC+14) rather than relying on hidden device state. All 6 passed against the unmodified implementation — confirming "protected" status without needing a code change. Build and all tests pass.)
- [x] Character titles and badges that reflect training style, not only level. (Kept `ProgressionService.title(for:)` as the level-gated title — unchanged, still used everywhere it already was — and added a separate `TrainingStyle` badge on top: a new enum with 7 muscle-group-driven styles (The Brawler/chest, The Anchor/back, The Powerhouse/legs, The Titan/shoulders, The Grappler/arms, The Ironclad/core, The Marathoner/cardio) plus `.allRounder` (no single group holds ≥35% of total muscle XP) and `.freshRecruit` (no muscle XP logged yet). `TrainingStyleService.style(for:)` derives it purely from `MuscleProgress.totalXP` distribution — replaces `CharacterProgressView`'s old plain-text `buildInsight` computed property (which did similar but untested inline logic) with a proper icon+name+detail badge row. Covered by a new `TrainingStyleServiceTests` suite (fresh-recruit both for empty/zero-XP muscles, each muscle group's dedicated style, balanced/all-rounder, and a boundary case just under the 35% dominance threshold); build and all tests pass. Verified live in the simulator: the seeded preview character (Legs-heavy) correctly shows "The Powerhouse" with the walking-figure icon and "Your build leans Legs-dominant" detail.)
- [x] Build analysis insights in `CharacterProgressView`, such as push/pull balance and neglected muscle groups. (Added `TrainingInsightsService.insights(for:)`, purely derived from `MuscleProgress.totalXP` with no persisted state of its own. Push/Pull Balance splits Arms XP evenly between push and pull (chest+shoulders+½arms vs. back+½arms, since arms cover both triceps and biceps) and reports push-heavy/pull-heavy/well-balanced once one side holds ≥65% of combined push+pull XP. Neglected Muscle Group flags whichever group's XP falls below 30% of the across-all-groups average — deliberately silent for a brand-new character (needs `average > 0`) so it doesn't declare every group "neglected" before any training has happened. Both are optional (return `nil`) rather than always showing, so the section only appears once there's something meaningful to say. Added an "Insights" section to `CharacterProgressView`, styled like the existing Personal Records rows. Covered by a new `TrainingInsightsServiceTests` suite (empty/zero-XP no-op, push-heavy/pull-heavy/balanced detection, arms' even 50/50 split, neglected-group detection, and no-false-positive when reasonably even); build and all tests pass. Verified live in the simulator: both insights render correctly for the seeded preview character, correctly identifying Chest as the neglected group (lowest XP).)
- [x] Daily or weekly quests generated from recent training history. (Added `SuggestedQuestService.suggestedQuest(from:lookbackDays:now:)`, purely derived from completed-quest history (30-day lookback, no persisted state). Groups completed quests by name; among names repeated 2+ times, picks whichever one's most recent occurrence is oldest — i.e. the routine that's gone longest without being repeated, so it reads as "due again." Snapshots that routine's exercises from its most recent occurrence via the existing `QuestExerciseBlueprint`/`QuestTemplateService.blueprint(from:)` (refactored `QuestTemplateService.makeQuest` to share its blueprint-to-Quest builder with this service, rather than duplicating the exercise/set construction loop). Returns nil whenever there's no established repeating pattern yet (fresh install, or every quest under a distinct name), so nothing is ever generated from insufficient history. Added a "Suggested Quest" card to `QuestDashboardView`, shown only when there's no active quest, with a "Start Suggested Quest" button that builds a new active `Quest` from the suggestion in the character's preferred weight unit. Covered by a new `SuggestedQuestServiceTests` suite (no history, all-unique names, single quest, correct "most overdue" selection between two repeated routines, exercise snapshot from the latest occurrence, lookback-window exclusion, ignoring non-completed quests, and `makeQuest` producing a correctly-unit-tagged active quest); build and all tests pass. Verified live in the simulator: no crash, dashboard renders correctly; the card's `activeQuests.isEmpty` gate was confirmed correctly hidden while an active "Core Trial" quest exists. Seeing the card itself render live would require completing quests under a repeated name via taps, which this environment can't drive interactively — verified via the unit test suite instead.)
- [ ] Expand the monster/boss pool and add more background scenes as level ranges grow (see `RPGMonsterRegistry`, `RPGBossRegistry`). (**Blocked, left unchecked**: every monster/boss entry needs a full hand-drawn sprite set (idle/attack/hit/defeat) and new background scenes need new hand-drawn PNGs. `scripts/generate_pixel_assets.py` is explicitly deprecated — the project now requires manually-created art dropped in `ArtSource/RPG/incoming/` and imported via `scripts/import_rpg_art.py` — and no image-generation tool is available in this environment to produce new sprites/backgrounds. Checked `ArtSource/RPG/incoming/`: it only contains source files for monsters/bosses/classes/equipment/skills already fully cataloged in `RPGMonsterRegistry`/`RPGBossRegistry`/`Assets.xcassets`, nothing new and unimported. Also noted in passing: the 40-49 and 50+ monster level bands currently share one background, `rpg_bg_ruined_castle` (`MonsterSpawnService.backgroundAsset(forLevel:)`) — a real gap once new art exists. Skipping to the next actionable item.)

## P2 - History, Analytics, And Recovery

- [x] Calendar view for completed quests. (Added `QuestCalendarService` with two pure helpers: `groupedByDay(_:calendar:)` (buckets completed quests by `calendar.startOfDay`) and `monthGrid(containing:calendar:)` (a padded 42-day/6-week grid for a given month, aligned to `calendar.firstWeekday` so it works correctly regardless of locale). Added `QuestCalendarView`: a month grid with prev/next navigation, a gold dot on any day with at least one completed quest, a gold outline on today, tap-to-select highlighting, and a list of that day's quests (name + XP, tappable into `QuestDetailView`) below the grid. `QuestHistoryView` gained a segmented List/Calendar picker in the navigation bar; the existing list view is otherwise unchanged. Covered by a new `QuestCalendarServiceTests` suite (same-day grouping, incomplete-quest exclusion, empty input, 42-day grid length, every day of the month present, grid starts on the configured first weekday, grid is fully consecutive with no gaps) using an explicit UTC calendar so the tests are timezone-independent; build and all tests pass. Verified live in the simulator: the History tab's new List/Calendar segmented control renders and defaults to List correctly, showing the seeded completed quests. Couldn't tap through to the Calendar segment itself and screenshot the grid — no UI-automation tooling available in this environment — verified that code path via the unit test suite instead.)
- [x] Weekly and monthly XP, volume, and consistency charts. (Added `TrainingChartsService.periodStats(from:period:periodsCount:calendar:now:)`, purely derived from completed-quest history: builds the last N weeks/months ending at `now` (oldest first), each with total XP, total volume (reps × weight across completed sets, normalized to pounds via the existing `WeightUnit.convert` regardless of which unit a set was logged in — matching `AchievementService`'s volume-counting convention), and days-trained (distinct calendar days, not quest count, so two quests on the same day don't double-count). Periods with zero activity are still included so a chart never silently skips a quiet week/month. Added `TrainingChartsView` using Swift Charts (`import Charts`, iOS 17+, no explicit framework linking needed — confirmed it links automatically) with a Week/Month segmented toggle and three bar charts (XP, Volume, Days Trained). `QuestHistoryView`'s List/Calendar picker became a three-way List/Calendar/Charts segmented control. Covered by a new `TrainingChartsServiceTests` suite (zero-activity periods still present, correct oldest-first ordering, same-period XP summing, out-of-window exclusion, kg→lb volume normalization, completed-sets-only volume, distinct-day counting, incomplete-quest exclusion, cross-month-boundary grouping) using an explicit UTC/fixed-first-weekday calendar for determinism — caught and fixed a week-boundary test bug during development (two dates that were actually adjacent-but-different ISO weeks); build and all tests pass. Verified live in the simulator: the History tab's segmented control now correctly shows three options (List/Calendar/Charts) with no crash. Couldn't tap into the Charts segment itself and screenshot the rendered bar charts — no UI-automation tooling available in this environment — verified that code path via the unit test suite instead.)
- [x] Muscle group heatmap showing recent training load and recovery balance. (Added `MuscleRecoveryService.loadStats(from:calendar:now:)`, purely derived from completed-quest history: for each muscle group, sums a 7-day recent-load figure (reusing `ProgressionService.exerciseXP(_:)`, with the same primary=100%/secondary=40% split `ProgressionService.distributeXP` already uses, so this stays consistent with how XP is actually attributed elsewhere) and tracks days-since-last-trained across *all* history, not just the lookback window, so a muscle untouched for a month still reports its real recency instead of silently reading as "never trained." Maps that recency into a `RecoveryStatus` (untrained / fatigued ≤1 day / recovering ≤3 days / fresh — a simple stand-in for the common ~48-72 hour strength-recovery heuristic). Added a "Recovery" section to `CharacterProgressView`, styled as a heatmap: each muscle group's panel is tinted (silver/red/orange/green) by its status, with an icon, status label, and "N days ago" detail. Covered by a new `MuscleRecoveryServiceTests` suite (all groups present with no history, fatigued/recovering/fresh day-boundary cases, recent-load correctly excludes quests outside the lookback window while days-since still counts them, secondary-muscle reduced load share, incomplete-quest exclusion, most-recent-occurrence wins when trained multiple times); build and all tests pass. Verified live in the simulator: the Recovery section renders correctly on the Character tab, showing Chest as untrained (gray) and Back as fatigued (red) — matching the seeded preview quests completed today.)
- [x] Quest search and filters by date, muscle group, exercise, status, and XP. (Added `QuestFilterCriteria` (search text + optional muscle group/status/date range/XP range, with an `isActive` flag) and `QuestFilterService.filter(_:criteria:)`, a pure AND-combination of all set dimensions — search text matches either the quest name or any exercise name (covering the "exercise" filter dimension without a separate picker), muscle group matching checks both primary and secondary muscles. Wired into `QuestListView`: added `.searchable(text:)`, a "Filters" toolbar button (fills in when any filter is active) opening a new `QuestFilterSheet` (status/muscle-group pickers, optional after/before date toggles, optional min/max XP steppers, and a "Clear Filters" action that preserves the search text). `QuestListView` normally shows only non-completed quests (its existing, unchanged default) — but since "status" is one of the filterable dimensions and would be meaningless if completed quests stayed excluded, once any filter/search is active the source list switches to *all* quests regardless of status. Covered by a new `QuestFilterServiceTests` suite (inactive-by-default, any single field activates, name search, case-insensitive exercise-name search, primary/secondary muscle matching, status filtering, start/end date filtering, XP range filtering, and multiple criteria combining with AND); build and all tests pass. Verified the app still launches and runs without crashing (screenshot-checked); `QuestListView` itself sits behind a "View All Quests" tap from the Dashboard, so the live search bar/filter sheet couldn't be screenshotted directly — no UI-automation tooling available in this environment — verified via the unit test suite instead.)
- [x] Notes and perceived effort on quests and individual exercises. (`Exercise` already had a `notes` field surfaced in `ExerciseLoggingView`; added the missing counterpart — `Quest.notes: String` — plus a new `perceivedEffort: Int?` (1-10 RPE scale) on *both* `Quest` and `Exercise`. Added a shared `PerceivedEffortPicker` component (Views/Components) used in both places. `QuestDetailView` gained a "Journal" section (notes TextField + effort picker); `ExerciseLoggingView` gained the effort picker next to its existing notes field. Deliberately made both stay editable even when `isReadOnly` (i.e. after quest completion) — they're a post-workout reflection, not factual workout content that affects derived progression, unlike name/date/sets which lock. Deliberately did **not** carry `notes`/`perceivedEffort` into `QuestDuplicationService.duplicate(_:)` — a note about how *that specific session* felt, or how hard *that specific session* was, shouldn't pre-fill a brand-new, not-yet-happened workout (unlike sets/reps/weight, which are legitimately reusable defaults); confirmed via a new regression test. Also intentionally did **not** add these to `QuestTemplateService`/`QuestExerciseBlueprint` for the same reason — templates are reusable plans, not session journals. Covered by a new `QuestJournalTests` suite (defaults, round-trip get/set, and the duplication-doesn't-carry-over regression); build and all tests pass. Verified the app still launches without crashing on a completely fresh install (uninstall/reinstall, exercising the new SwiftData fields end-to-end) — couldn't tap into `QuestDetailView`/`ExerciseLoggingView` themselves to screenshot the new Journal section live, no UI-automation tooling available in this environment, verified via the unit test suite instead.)
- [x] Deload or recovery recommendations based on recent volume and streak patterns. (Added `RecoveryRecommendationService.recommendation(from:calendar:now:)`, purely derived from completed-quest history — no persisted state. Reuses the existing `RPGProgressionSnapshot.streak(from:)` to flag a `.restDay` suggestion once the current daily streak hits 7 consecutive days (matching the streak threshold already used elsewhere for achievements/milestones), and reuses `TrainingChartsService.periodStats(period: .week)` to flag a `.deloadWeek` suggestion once weekly training volume has risen strictly for 3 consecutive weeks straight (a common evidence-based deload-cadence guideline) — requiring nonzero volume each week so it reads as a genuine ramp-up, not just resuming after a break. Rest-day (acute) takes priority over deload-week (a slower trend) when both would apply. Falls back to `.allClear` otherwise. Added a recommendation banner to `CharacterProgressView`, shown only when there's an actual recommendation (not `.allClear`), placed right above the existing Recovery heatmap section since they share the same underlying signal (recent training load). Covered by a new `RecoveryRecommendationServiceTests` suite (no-history all-clear, 7-day-streak triggers rest day, a 3-day streak doesn't, 4 weeks of strictly rising volume triggers deload, flat/declining volume doesn't, rest-day priority when both conditions hold, incomplete-quest exclusion); build and all tests pass. Verified the app still runs without crashing after the schema-adjacent change (screenshot-checked) — the banner itself lives behind the onboarding flow (gated by a completed first-run flow needing a tap, which this environment can't drive), so it's verified via the unit test suite instead. This completes the "P2 - History, Analytics, And Recovery" section.)

## P2 - UX And Visual Polish

- [ ] Replace SF Symbol muscle placeholders with custom pixel-art muscle icons. (**Blocked, left unchecked**: `MuscleGroup.iconName` (Models/MuscleGroup.swift) already carries a comment marking these as SF Symbol stand-ins pending "custom pixel-art muscle group glyphs." Same constraint as the monster/boss/background item above — new hand-drawn PNGs are required, `scripts/generate_pixel_assets.py` is deprecated in favor of manually-created art via `scripts/import_rpg_art.py`, and no image-generation tool is available in this environment. Confirmed no muscle-icon art exists anywhere in `ArtSource/` or `Assets.xcassets` yet. Skipping to the next actionable item.)
- [x] Replace generated app icon with a hand-drawn pixel-art icon before shipping. (Resolved outside this loop — `AppIcon.appiconset/AppIcon.png` is now a real 1024×1024 pixel-art icon (a green slime lifting a barbell, matching the game's RPG pixel-art aesthetic) committed separately as `aea379a`, replacing the previous 10,580-byte Xcode-generated placeholder. Noticed and checked off while verifying an unrelated commit's diff didn't touch it.)
- [x] Define a pixel-art visual spec: palette, border weights, corner radius, shadows, icon grid size, and typography usage. (Formalized this as living documentation directly in `RepSetForgeTheme.swift` — a "Pixel-Art Visual Spec" doc-comment block anchored to the values already defined there, not invented from scratch: palette usage rules for each `questNavy`/`questGold`/`questSilver`/`questGreen`/`questRed`/`questParchment` color (semantic role, not just hex value), typography usage rules for `.title`/`.heading`/`.body`/`.stat` (when to use which), the existing `cornerRadius`/`borderWidth` constants behind `pixelPanel()`, and the icon/sprite grid-size table already established as the source of truth in `ArtSource/RPG/README.md`'s "Size Summary" (native design size → 4x exported PNG size, per category: hero/small monster/medium monster/large monster/boss/equipment/skill icon/background). For shadows — previously completely undocumented and used exactly once in the whole codebase with hand-typed magic numbers (`RPGSceneView`'s floating combat text: `.shadow(color: .black.opacity(0.8), radius: 0, x: 1, y: 1)`) — added a new `pixelTextShadow(opacity:)` view modifier alongside the existing `pixelPanel()` to make the "hard, zero-blur, 1pt-offset" rule reusable and enforced in code rather than just described in prose, and updated that one call site to use it. Deliberately did not add a `RepSetForgeIconSpec.exportScale` constant for the icon-size multiplier since nothing in the runtime code actually computes with it (export scale is purely an asset-authoring-time detail, not a display-time one) — an unused constant would be premature abstraction, so the size table lives as a comment instead. Build and all tests pass; verified the app still runs without crashing and the refactored shadow call site is value-identical (same radius/offset/opacity), so no visual regression risk.)
- [x] Apply pixel-art polish consistently across quest cards, stat panels, badges, XP bars, buttons, empty states, and completion rewards. (Audited every `Pixel*` component plus the empty-state texts against the visual spec just defined and fixed four real inconsistencies (not a rewrite — most components, including `PixelStatPanel`, `PixelBadge`, and `PixelButtonStyle`, already followed the spec correctly): (1) `QuestCompletionRewardRow`'s "LEVEL UP!" tag used a fully-rounded `Capsule()`, breaking the spec's "squared-off, never a capsule" rule — the clearest violation found — changed to a `RoundedRectangle`. (2) `PixelXPBar` and (3) `PixelQuestCard`'s status stripe both hand-typed `cornerRadius: 3` as an unexplained magic number instead of a named constant; added `RepSetForgeMetrics.cornerRadiusSmall` (documented in the spec alongside the existing `cornerRadius`/`borderWidth`) and pointed both at it, along with the newly-fixed reward-row tag. (4) Two Form-based "Manage Templates" empty states (`QuestDetailView`, `QuestListView`) used plain `.foregroundStyle(.secondary)` with no explicit font, inconsistent with every other empty state in the app (`RepSetForgeFont.body()` + `Color.questNavy.opacity(0.6)`) — brought both in line. Build and all tests pass. Verified live in the simulator — and, since the onboarding gate (tap-only, no automation available) had blocked screenshot verification across several prior tasks, discovered and used a real fix: RepSetForge's SwiftData store is a plain SQLite file (`default.store`) in the app's data container, so `UPDATE ZPLAYERCHARACTER SET ZHASCOMPLETEDONBOARDING = 1` via `sqlite3` unblocks it without needing a tap, letting most other screens become screenshot-reachable again (saved as a memory for future cycles). Confirmed via screenshot that the Dashboard's stat panel XP bar and "Current Quest" card status stripe both render correctly with the new shared corner radius, and the Achievements screen's badges are unaffected; the reward-row and empty-state fixes are simple, low-risk value-preserving changes verified by successful build rather than live screenshot, since reaching them needs a completed-quest flow or several more taps.)
- [x] Add lightweight set-complete, XP-gain, level-up, achievement-unlock, and quest-complete animations. (Added a reusable `staggeredAppearance(index:)` view modifier to `RepSetForgeTheme.swift` (documented in the visual spec alongside `pixelPanel()`/`pixelTextShadow()`) — a lightweight fade+8pt-slide entrance with a small per-index delay, so a list of freshly-earned rewards cascades in rather than popping in all at once; it checks `accessibilityReduceMotion` itself so call sites don't need their own branch. Applied it to every reward-adjacent list on `QuestCompletionView`: XP rows (character/muscle/gold), level-up entries, achievement-unlock cards, new-PR rows, and equipment-drop rows — covering XP-gain, level-up, and achievement-unlock in one consistent sweep. Quest-complete gets its own distinct treatment: the header's checkmark-seal icon scales in from 0.4→1 with a spring on appear (gated on Reduce Motion manually, since it's a one-off rather than a list). Set-complete uses `.symbolEffect(.bounce, value: set.completed)` on the checkbox icon in `ExerciseLoggingView`'s `ExerciseSetRow` — a native, built-in "pop" that doesn't touch layout or any other control, so it can't block fast logging; wrapped in an explicit Reduce Motion check since `symbolEffect` doesn't disable itself automatically. Build and all tests pass (animations are UI-layer only, no new testable logic). Verified the app still runs without crashing after the changes (screenshot-checked, using the SQLite onboarding-bypass technique from the previous task) — `QuestCompletionView` itself is only reachable by actually completing a quest (a tap-triggered action, not a revisitable screen, so not even the SQL workaround helps), consistent with every prior task touching this screen; verified via successful build and code review instead.)
- [x] Ensure animations respect Reduce Motion and never block fast workout logging. (Audited every `.animation(`/`withAnimation(`/`.symbolEffect(`/`.transition(`/`.scaleEffect(`/`.offset(` usage in the codebase. Found the existing handling was already correct almost everywhere — `RPGSceneView`'s hit/attack offsets, the floating combat text's rise, and both new modifiers added last cycle (`staggeredAppearance(index:)`, the completion-header spring, the set-complete `symbolEffect`) all already gate the actual *positional/scale* motion behind `accessibilityReduceMotion`, matching this codebase's established pattern. Found exactly one place worth a documented decision rather than a code change: `PixelButtonStyle`'s 3% press-down `scaleEffect` isn't gated, but it's synchronous direct-touch feedback (not an independent animation playing out on its own), the same category of feedback system buttons keep regardless of the setting — Reduce Motion is about parallax/movement that plays on its own, not touch-synchronous squish. Added a comment explaining that reasoning at the call site instead of leaving it looking like an oversight, plus a matching addition to the visual spec in `RepSetForgeTheme.swift` drawing that line explicitly (gate independent motion; direct-touch feedback and simple opacity crossfades don't need it) and a companion rule that no animation may gate/disable a logging control (set checkboxes, reps/weight fields, Add Set) — confirmed true today: the rest-timer banner in `ExerciseLoggingView` runs alongside logging, never blocking it, unchanged from when that guarantee was originally built. No behavior changes were needed — this was a genuine audit that confirmed prior work was already sound, in the same spirit as the earlier streak-protection audit task. Build and all tests pass; verified the app still runs without crashing after the comment-only changes.)
- [x] Add a visual QA checklist with simulator screenshots for Dashboard, Quest Detail, Character, History, Achievements, and Completion. (Added `Docs/VisualQAChecklist.md` with real, freshly-captured `--preview-data` screenshots checked into `Docs/qa-screenshots/` for the 4 screens directly reachable via `--tab N` launch args — Dashboard, Character, History, Achievements — each with a specific per-element review checklist, not just a screenshot dump, plus the re-capture command so this stays maintainable as a living regression reference. Quest Detail and Completion are documented with the same level of detailed checklist but explicitly marked "requires manual QA in Xcode/Simulator": Quest Detail is a `NavigationLink` push destination and Completion only appears as the direct result of tapping "Complete Quest" (not a revisitable, data-driven screen) — neither has a deep-link/URL-scheme mechanism to reach programmatically, and building one solely for QA screenshot purposes would be scope creep beyond what this item asks for. Along the way, discovered and used the SQLite onboarding-bypass technique from a recent task to get real, uncropped screenshots of the Character screen's Recovery section and other previously-hard-to-reach states. No source code changed; build and all tests still pass as a sanity check before committing.)
- [x] Improve empty states for first launch, no history, and locked achievements. (Three targeted fixes, not a redesign — most empty states already existed and were reasonable. **First launch**: `QuestDashboardView`'s "no active quest" panel now distinguishes a genuinely fresh character (`completedQuestCount == 0`) from a returning player just between quests — "Ready for your first quest?" plus explicit next-step guidance ("Tap 'Start New Quest' below, add a skill or two, and log your first set...") instead of the same generic message either way. **No history**: `QuestHistoryView`'s single blind `.overlay` was showing "No completed quests yet." on top of *any* selected segment, which would have visually collided with the Calendar's month grid (still worth browsing even with no history) — scoped the overlay to List/Charts only, left Calendar alone (its own per-day empty message already covers that case), and added a Charts-specific second line ("Charts fill in once you complete your first quest.") explaining why the bars are empty rather than looking broken. **Locked achievements**: `AchievementsView` gained an "X of Y Unlocked" progress header with an encouraging subtitle that adapts to state (all-locked / partial / all-unlocked), so a wall of gray lock icons reads as visible progress toward a known total instead of undifferentiated failure. No new files/tests — this is UI-layer copy and conditional display logic; build and all tests pass. Verified all three live in the simulator using the SQLite onboarding-bypass technique, additionally clearing the seeded quest rows via `sqlite3` to reach the *true* zero-quest first-launch state (not just the `--preview-data` seeded-but-planned state) — confirmed the new Dashboard copy, the "0 of 13 Unlocked" header, and the unchanged List empty state all render correctly; Calendar/Charts modes need a tap to reach and weren't directly screenshotted, verified via code review instead.)
- [x] Add accessibility pass: Dynamic Type, VoiceOver labels, contrast, and large tap targets. (**Dynamic Type** — the biggest finding: `RepSetForgeFont`'s four font functions all used fixed `.system(size:)` points, meaning *none* of the app's custom typography scaled with the user's text-size setting at all. Fixed with one surgical change: a private `scaled(_:)` helper wrapping every size in `UIFontMetrics.default.scaledValue(for:)` before constructing the font — same call-site API everywhere else in the app, zero other files touched, and it re-resolves correctly on every body re-evaluation (confirmed live: set the simulator to `accessibility-extra-large` via `xcrun simctl ui <device> content_size accessibility-extra-large` and screenshotted the Dashboard — every custom-font label scaled up and wrapped correctly, vs. staying pinned at design-time size before this fix). **VoiceOver labels** — audited every icon-only control (no accompanying `Text`/`Label`) in the app; found and fixed 4: the Calendar's prev/next month chevrons ("Previous/Next month"), each calendar day cell (now announces the full date + "N quests completed"/"No quests completed" instead of just a bare number), the per-suggestion history-chart button in `AddExerciseSheet` ("View history for `<name>`"), and — highest-value since it's the core logging interaction — the set-completion checkbox in `ExerciseLoggingView` (now announces "Set N" / "Complete" or "Not complete" instead of an unhelpful raw glyph name). Also gave `PixelAchievementCard` one combined, explicit announcement ("Locked. `<detail>`" / "Unlocked `<date>`. `<detail>`") instead of relying on the badge's ambiguous default SF Symbol label to imply lock state. **Large tap targets** — added `.frame(minWidth: 44, minHeight: 44).contentShape(Rectangle())` to the calendar chevrons, calendar day cells (previously a 32pt row height, now 44pt), and the set-completion checkbox, meeting Apple's 44×44pt minimum; deliberately left the small inline "view history" icon button in the autocomplete chip row at its default `.bordered` size rather than forcing 44pt, since that would create a visually mismatched height against its sibling chip in the same compact HStack — an intentional secondary/dense-UI trade-off, not an oversight. **Contrast** — did a lighter-touch review rather than computing WCAG ratios for every color pair: the app's opacity-based secondary-text convention (`Color.questSilver.opacity(0.6)` etc.) matches the same reduced-emphasis-hierarchy pattern as iOS's own `.secondary`, and no critical information is conveyed *only* through low-contrast text (status is always paired with icon/color/position) — flagging a full numeric contrast audit as a good follow-up if precise WCAG AA compliance is required, rather than guessing at fixes here. No new files; build and all tests pass.)
- [x] Add haptics for set completion, level up, achievement unlock, and quest completion. (Used SwiftUI's `.sensoryFeedback(_:trigger:)` (iOS 17+) throughout rather than raw `UIImpactFeedbackGenerator` calls. Set completion (`ExerciseSetRow`) gets a light `.selection` tick, gated to fire only on the false→true transition (not on un-checking) — deliberately the lightest feedback in the app since it repeats constantly through a workout and shouldn't fatigue. `QuestCompletionView` layers three feedbacks off the same `headerAppeared` trigger: a base `.success` for quest completion (always), `.impact(weight: .heavy)` additionally when a level-up occurred, and `.impact(weight: .medium)` additionally when an achievement unlocked — so a quest that does more (level up AND unlock something) produces visibly more feedback than a routine completion, without needing dedicated per-event haptic types that don't exist in the `SensoryFeedback` API. Documented the convention (frequent-action → light/selection, celebratory-and-rare → layered success+impact-by-weight) in `RepSetForgeTheme.swift`'s visual spec so future haptic additions stay consistent. No new files/tests — haptics are physical output with no testable logic; build and all tests pass. Verified the app still runs without crashing after the changes (screenshot-checked); haptics themselves can't be verified via screenshot or even a live tap in this environment, since the iOS Simulator has no Taptic Engine and silently no-ops `.sensoryFeedback` regardless of tap automation — build success and code review are the correct verification method here, not simulator screenshots. This completes the entire "P2 - UX And Visual Polish" section.)

## P2 - Data Portability And Integrations

- [x] Export progress to JSON or CSV. (Added `ProgressExportService` (pure, no persisted state) with a full `Codable` `ProgressExport` DTO tree (character summary, muscle levels, quests with their exercises/sets, personal records, achievements) independent of the SwiftData models, so the export format never breaks if the models change shape later. Two output paths from the same DTO: `json(from:)` (pretty-printed, sorted-keys, ISO-8601 dates) for a complete structured backup, and `csv(from:)` (one row per logged set — quest/exercise columns repeated per row, since a set is the finest-grained unit that fits a flat table) for spreadsheet analysis, with proper RFC-4180 field escaping for names containing commas/quotes. Added a "Data" section to the Character screen's Settings sheet: a Format picker (JSON/CSV), a "Prepare Export" button that writes to a temp file, then a `ShareLink` to save/share it once ready (a deliberate two-step flow rather than fighting `ShareLink`'s requirement for an item to exist upfront). This is export-only, matching the TODO's scope — the separate "Import progress from a previous export" item (still unchecked, right below this one) is where round-tripping back in gets built. Covered by a new `ProgressExportServiceTests` suite (empty-data export, character summary correctness, exercise/set structure preservation, JSON encode-then-decode round-trip, CSV header-plus-one-row-per-set counting, comma/quote escaping, empty-quests CSV is header-only); build and all tests pass. Verified the app still runs without crashing after the new `@Query`s in the Settings sheet (screenshot-checked); the export UI itself sits one tap behind the Settings gear icon, which this environment can't drive — verified via the unit test suite instead.)
- [x] Import progress from a previous export with conflict handling. (Scoped to JSON only — CSV is a lossy flat projection with no natural round-trip target, so it stays export-only; JSON is the structured format meant for backup/restore. **Conflict handling** is the core design point: rather than trying to merge scalar counters (character level/XP/gold, muscle levels, achievement unlock state) field by field, `ProgressImportService.importExport(from:context:)` only ever inserts *new* `Quest` rows — matched by the original `Quest.id`, now included in the export (`QuestExport.id`) specifically so re-importing the same file, or a second export whose history overlaps an earlier one, correctly detects "already have this" and skips it instead of duplicating — then calls the existing `ProgressionRebuildService.rebuild(context:)` to recompute every derived stat from scratch, exactly the same "rebuild from history, don't patch totals" architecture the rest of the app already uses for undo/edit. This means conflict handling for everything *except* raw quest rows is free and automatically correct. Discovered and fixed a design gap in the previous export commit along the way: `ExerciseExport`'s `primaryMuscle`/`secondaryMuscles`/`exerciseType` were stored as human-readable display names, which don't round-trip cleanly back to `MuscleGroup`/`ExerciseType` — changed those three fields to their stable `rawValue`s (both enums are already `Codable`), leaving genuinely display-only fields (quest status text, muscle/record-type summaries) as display names since those are never parsed back. Quest status on import is derived from `completedDate`/`date` via the existing `QuestScheduler.status(for:)` rather than parsed from the exported status string, which is both simpler and more robust. Added an "Import Progress…" button to the same Settings "Data" section as the export UI, using `.fileImporter` (JSON only) with a result message showing imported vs. skipped-duplicate counts. Covered by a new `ProgressImportServiceTests` suite (fresh insert, XP correctly recomputed from imported quests, re-importing the same file is a no-op the second time, a second export with partial overlap only adds the new quest, invalid/corrupt data throws a clear error, a future-dated quest without a completedDate correctly imports as `.planned` via `QuestScheduler`); build and all tests (including the pre-existing export tests, unaffected by the raw-value fix) pass. Verified the app still runs without crashing after the new `.fileImporter` wiring (screenshot-checked); the import UI sits behind the same tap-gated Settings sheet as export, verified via the unit test suite instead. This completes the export/import pair within "P2 - Data Portability And Integrations" — HealthKit, Shortcuts/App Intents, iCloud sync, and privacy settings are still unchecked below.)
- [x] HealthKit integration for workout sessions, active energy, heart rate, and body metrics. (Added the `com.apple.developer.healthkit` entitlement (`RepSetForge/RepSetForge.entitlements`, wired via `CODE_SIGN_ENTITLEMENTS` in `generate_project.py`) plus the required `NSHealthShareUsageDescription`/`NSHealthUpdateUsageDescription` privacy strings — the riskiest part of this task, since a hand-edited pbxproj entitlements change could have broken the whole build; verified it didn't by building clean afterward (`import HealthKit` and the entitlement linked/codesigned with no explicit Frameworks-phase registration needed, the same auto-linking behavior already seen with Charts). `HealthKitService` requests read access to workouts/active energy/heart rate/body mass and write access to workouts/active energy; `saveWorkout(start:end:)` writes a completed quest as an `HKWorkoutBuilder` traditional-strength-training session with an *estimated* active-energy sample (standard MET formula: kcal = 5.0 × bodyWeightKg × hours, using the most recent Health body-mass reading if available, falling back to 70kg) — explicitly documented as an estimate, not a measured figure, since this app has no wearable sensor of its own. `workoutDateRange(for:)` approximates a session's start time backward from its completion time (5 minutes per completed set) since individual sets aren't independently timestamped; this and the MET-formula function are pure and fully unit-tested. Wired into `QuestDetailView.completeQuest()` as fire-and-forget (`try?` inside a `Task`) so a denied/unavailable Health integration can never block completing a quest — deliberately computes the date range *synchronously* on the caller's side and passes only plain `Date` values into the `Task`, since `Quest` is a SwiftData model and not `Sendable`, and capturing it directly would risk a Swift 6 strict-concurrency violation. Added a "Connect to Apple Health" button plus an explanatory note to the Settings sheet, shown only when `HealthKitService.isAvailable`. Covered by a new `HealthKitServiceTests` suite for every pure function (MET-formula scaling by duration/weight, exact formula check, zero-duration edge case, nil for a non-completed quest, duration scaling with completed-set count across multiple exercises, zero-sets-still-one-set-worth-of-duration); build and all tests pass. The actual authorization prompt and read/write against real Health data fundamentally cannot be verified in this environment — there's no physical device, no Health app data, and no way to grant a system permission dialog even with tap automation — so this was verified as far as is possible here: a clean build with the entitlement/linking correctly wired, all pure logic unit-tested, and no crash launching the app. Live end-to-end verification (does a completed quest actually appear in Health) needs a real device and is noted here as a manual QA follow-up.)
- [x] Shortcuts/App Intents for starting a quest, logging a set, and viewing current level. (Split into `AppIntentService` (pure, `ModelContext`-parameterized logic, matching every other service in this codebase and fully unit-testable) and thin `AppIntent` wrappers in `RepSetForgeShortcuts.swift` that bridge to `PersistenceController.shared` — the same container the running app uses. `startQuest` inserts a new active quest (mirrors `QuestDashboardView.startNewQuest()`); `logSet(exerciseName:reps:weight:weightUnit:)` finds the current active/planned quest and appends a completed set to a matching exercise (case-insensitive), creating the exercise if needed — and when creating one, reuses the primary/secondary muscles and type from the most recently logged exercise of that name *anywhere* in history if one exists, so a Shortcut never has to ask "which muscle group?" for something already logged before, falling back to chest/strength only for a genuinely new name; `currentLevelSummary` reads the character's level/title. Registered via an `AppShortcutsProvider` with Siri phrases for all three. Hit and fixed two real Swift 6 strict-concurrency issues along the way: the `AppIntent` structs can't be `@MainActor` at the type level (conflicts with `AppShortcutsProvider.appShortcuts` constructing them from a synchronous, non-isolated context) — each `perform()` hops to the main actor itself via `MainActor.run` instead, since `PersistenceController` is `@MainActor`-isolated; and `MainActor.run`'s closure must return a `Sendable` value, so `StartQuestIntent` explicitly discards the non-`Sendable` `Quest` it gets back rather than letting it flow out of the closure. Also added the entitlement-adjacent build risk this task shares with the HealthKit item: verified the build stayed green with real `import AppIntents` usage (auto-linked, same as Charts/HealthKit). Covered by a new `AppIntentServiceTests` suite (quest insertion, throwing with no active quest, new-exercise creation, appending to an existing exercise case-insensitively, reusing historical muscle/type for a new exercise, defaulting to chest/strength for a genuinely new name, preferring a planned quest over a completed one, level-summary with and without a character); build and all tests pass. Verified live: the `appintentsmetadataprocessor` build step went from "No AppShortcuts found — Skipping" (before this change) to actually writing `Metadata.appintents`, concrete confirmation the shortcuts are correctly registered and will surface in the Shortcuts app — and the app itself still launches without crashing. The Shortcuts app's own UI (actually running a shortcut, seeing it in search) is a separate system app this environment has no way to tap through; that step needs manual QA on a real device.)
- [x] iCloud sync once the local SwiftData model is stable. (Previously deliberately deferred here since Phase 1 MVP was specified as local-only per `CLAUDE.md`; **the user explicitly instructed adding CloudKit as a priority now**, which supersedes that earlier deferral — a live, direct instruction from the user overrides a standing "not yet" default. Switched `PersistenceController`'s real (non-in-memory) `ModelConfiguration` to `cloudKitDatabase: .automatic`, which syncs through the container identifier declared in `RepSetForge.entitlements`; in-memory configs (previews, `inMemory: true` callers) explicitly stay `.none` since an in-memory store can't sync anyway. Getting there required a full audit of all 12 `@Model` classes: CloudKit requires every attribute be Optional or have a default value, so every non-optional/non-defaulted property across every model got a default (never actually relied on, since every `init(...)` already sets a real value immediately) — plus `Achievement.key`'s `@Attribute(.unique)` had to be dropped entirely since CloudKit doesn't support unique constraints (uniqueness is already enforced at the application level by `seedCoreDataIfNeeded()`'s existing-key check, so no functional change). Hit and fixed a second, stricter constraint the attribute-level fix didn't cover: CloudKit requires to-many *relationships* to be Optional at the stored-property level specifically — `Quest.exercises`/`Exercise.sets` had default values but weren't Optional, which crashed the app on launch with "CloudKit integration requires that all relationships be optional." Fixed with the standard private-optional-backing + public non-optional computed-accessor pattern (`exercisesStorage: [Exercise]?` backing a computed `var exercises: [Exercise] { get { exercisesStorage ?? [] } set { ... } }`), so every one of the dozens of existing call sites across the codebase kept working with a plain `[Exercise]`/`[ExerciseSet]` unchanged. Also hit a second real crash after fixing relationships: with no iCloud account signed into the simulator, `ModelContainer(for:configurations:)` still initializes successfully and the app runs fine — CloudKit's actual mirroring/sync setup happens asynchronously afterward and fails *gracefully* there (logged, not fatal), simply queuing until an account becomes available, so no fallback-container code was needed. Added CloudKit entitlements (`com.apple.developer.icloud-services`, `-container-identifiers`, `-container-environment`) to the existing `RepSetForge.entitlements` file (already wired via `CODE_SIGN_ENTITLEMENTS` from the HealthKit work) — confirmed via a successful build that no additional pbxproj capability registration was needed, matching how HealthKit's entitlement worked earlier. Tried adding `INFOPLIST_KEY_UIBackgroundModes` for push-triggered background sync per Apple's stated best practice, but confirmed via `PlistBuddy` inspection of the built Info.plist that this specific key doesn't get generated through the `INFOPLIST_KEY_` mechanism the way the multi-value orientation keys do — reverted it since a silently-ineffective build setting is worse than no setting; foreground/periodic sync still works without it, so this is a possible future enhancement, not a blocker. Updated the Settings sheet's Privacy & Data explanation text, which had said "nothing is sent to any RepSetForge server" — still true (there's no RepSetForge server), but now also syncs to the user's own private iCloud account, so the copy was corrected to say so explicitly rather than leave a now-inaccurate privacy claim in place. Build and all tests pass. Verified extensively in the simulator beyond the usual screenshot check, given the severity of what a mistake here could break: confirmed the app launches and reaches the Dashboard correctly (screenshot-checked) with real `--preview-data` seeded through the actual CloudKit-backed on-disk store (not just the isolated in-memory test containers), inspected the store's SQLite schema directly and confirmed real `ANSCK*` CloudKit-mirroring shadow tables now exist alongside the normal entity tables, and confirmed the 3 preview quests / 11 exercises seeded and persisted correctly through the new relationship structure. Re-verified the onboarding-bypass SQLite technique (`ZPLAYERCHARACTER`/`ZHASCOMPLETEDONBOARDING`) still works unchanged. This item can only be fully end-to-end verified — actual multi-device sync — on a real device signed into iCloud, which this environment can't provide; verified everything short of that.)
- [x] Privacy settings and clear local-data explanation. (Added a "Privacy & Data" section to the Settings sheet with two parts: a plain-language explanation of where data lives, and a real, working control rather than just explanatory text: "Delete All Workout Data" (destructive, behind a confirmation dialog explaining exactly what it does). Added `PrivacyDataService.deleteAllWorkoutData(context:)`: deletes every `Quest` (cascading to its `Exercise`/`ExerciseSet` rows via the existing cascade delete rule) then calls the already-tested `ProgressionRebuildService.rebuild(context:)` to reset character/muscle levels, gold, achievements, and personal records back to baseline — reusing that infrastructure rather than hand-resetting every field, consistent with this codebase's "rebuild from history, don't patch totals" pattern. Deliberately does *not* reset onboarding completion, class selection, or owned equipment/skills, since those are standing preferences, not workout history — "delete my data" shouldn't silently re-trigger onboarding or reset gear choices, and the UI copy says so explicitly. Covered by a new `PrivacyDataServiceTests` suite (full deletion cascades correctly, character/muscle XP resets to baseline, idempotent with no existing data, character/muscle rows themselves survive — only their values reset, not the rows); build and all tests pass. Verified the app still runs without crashing after the new section/confirmation-dialog wiring (screenshot-checked); the section itself sits behind the same tap-gated Settings sheet as export/import/Health, verified via the unit test suite instead. **Update**: once iCloud sync was added below, the explanation text originally written here ("nothing is sent to any RepSetForge server because there isn't one, data only ever leaves the device via export or Health") became inaccurate — data now also syncs to the user's own private iCloud account automatically. Corrected the copy to say so explicitly (still true that there's no RepSetForge server; only the user and Apple can see the synced data) rather than leave a stale privacy claim in place after an architecture change.)

## P3 - Platform Expansion

- [x] Apple Watch companion for fast set logging and timers. (Previously skipped given the scope/risk of a first-ever new native target; the user explicitly asked to proceed with the non-art tasks, which supersedes that. The phone/watch data-sharing question flagged as an open architecture decision when this was first evaluated turned out to already be resolved by the CloudKit work done earlier in this same backlog: the watch app just needs its own CloudKit-backed `ModelContainer` pointed at the identical container identifier, and the two devices converge via CloudKit sync — no WatchConnectivity or App Group needed at all. **Scope**: fast set-completion toggling and a rest timer, matching the TODO wording literally — not quest completion, XP, achievements, or gold, which stay phone-only; the watch app only mutates `ExerciseSet.completed`. **Critical invariant discovered and designed around**: the watch app must never run `PersistenceController`'s seeding logic — if it seeded its own `PlayerCharacter`/`RPGEncounterState` before the phone's had synced down, CloudKit would merge them as two separate rows, not one, since there's no server-side "only one of these" constraint, only application-level convention. `RepSetForgeWatchApp.swift` builds its own minimal container with no seeding at all, showing a graceful "No Active Quest" empty state if nothing's synced yet — documented in its own doc comment since this is exactly the kind of invariant that's invisible until it silently breaks. **New target mechanics**: hand-wired a full `RepSetForge Watch App` native target into `generate_project.py` (watchOS SDK/`WKCompanionAppBundleIdentifier`/its own entitlements+config), an "Embed Watch Content" `PBXCopyFilesBuildPhase` + target dependency on the iOS app target so one install carries both, and — the genuinely new wrinkle beyond the earlier UI-test-target precedent — sharing existing files (all 12 `@Model` classes, their 6 supporting enums, `RepSetForgeSchema.swift`, `AchievementService.swift`, `Fixtures.swift`) across *two* targets' Sources phases, which needs a second `PBXBuildFile` entry per shared file (a file reference is shared, but each target's Sources phase needs its own build-file entry pointing at it). Deliberately did **not** share `RepSetForgeTheme.swift` or `PersistenceController.swift` — the former is built on `UIColor`, unavailable on watchOS at all; the latter's seeding logic is exactly the invariant above. Added a small watch-only `WatchTheme.swift` (plain `Color(red:green:blue:)`, no `UIColor`) for the couple of colors actually used. No watch-specific Assets.xcassets/app icon — consistent with the app icon being one of the environment-blocked art items elsewhere in this list. Added a dedicated `RepSetForge Watch App.xcscheme` (hand-written, mirroring Xcode's own auto-generated structure) since the watch target needs its own scheme to build/launch independently of the phone's. Verified thoroughly given the size of this change: `xcodebuild build -scheme RepSetForge` (the documented, standard command) succeeds and correctly embeds+validates the watch app inside the phone app bundle; the full test suite still passes unchanged; `xcodebuild build -scheme "RepSetForge Watch App" -destination watchOS Simulator` succeeds standalone; installed and launched on a real Apple Watch Series 11 simulator with no crash, correctly showing the empty state (screenshot-checked) — this environment has no signed-in iCloud account on any simulator, so real cross-device CloudKit sync between phone and watch cannot be verified here, only that each side runs correctly and gracefully handles the no-data-yet case; and re-verified the phone app itself still launches and functions correctly after all the pbxproj surgery. Real sync verification needs two real devices signed into the same iCloud account — noted as a manual QA follow-up, the same category of gap as HealthKit/Shortcuts' live-behavior verification earlier in this list.)
- [x] Widgets for streak, active quest, and current level. (New `RepSetForgeWidgets` app-extension target (`com.apple.product-type.app-extension`, `com.apple.widgetkit-extension`), embedded into the phone app via a "PlugIns"/`dstSubfolderSpec = 13` copy-files phase — the widget-extension equivalent of the Watch companion's "Embed Watch Content" phase. **Data sharing, a real design decision**: unlike the watch app (which reads via CloudKit sync alone, tolerant of latency since it's a separate device), a widget's `TimelineProvider` runs under a strict WidgetKit execution budget where a CloudKit round-trip could time out — so this needed an **App Group–shared local store** instead, which the widget reads directly and synchronously. Added `group.dev.gnwn.RepSetForge` to the phone app's entitlements and a new `RepSetForgeWidgets.entitlements` with the same group (plus CloudKit, so the *phone's* copy of the data still syncs across the user's own devices as before — the App Group is purely a same-device fast-path, not a replacement for CloudKit sync). Refactored `PersistenceController`'s `ModelConfiguration` to store at the App Group container URL (via new `SharedStore.swift`, shared by phone + widget) instead of the default app-container location — verified this real change to existing, working persistence code carefully: confirmed via `find` that the store actually lives at `Containers/Shared/AppGroup/.../default.store` post-change, not silently falling back to the old location. Added `StreakService` (current unbroken streak length, distinct from `AchievementService`'s private "is there a streak of at least N days" check used only for unlocks) as a real, tested service — 6 new tests covering the "today is a rest day but streak still shows" and "stops at the first gap" edge cases. The widget's view is deliberately SwiftData-free (`RepSetForgeWidgetEntry` is a plain struct) — only the `TimelineProvider` touches SwiftData, so widget `#Preview`s need no `ModelContainer` at all, just sample entries. Hit and fixed two real, non-obvious extension-target bugs by actually building rather than just reasoning about it: (1) two brand-new phone-side files (`SharedStore.swift`, `StreakService.swift`) were registered in `APP_SOURCES` for compilation but never added to their `simple_group(...)` calls' children lists, leaving them orphaned from correct directory-relative path resolution — surfaced only when a *second* target (the widget) tried to compile them fresh, since the phone target's already-successful prior build masked it; (2) a latent bug in two new UUID-offset choices (`u(2, 500+i)`, `u(2, 700+i)`, etc.) — the `u()` helper's format string assumes a 2-hex-digit sequence number, and offsets above 255 silently produced a 25-character (not 24) UUID string, corrupting build-file identity; both the Watch and Widget shared-file UUID ranges had this defect, moved to safe `0xA0`/`0xC8`/`0xF0`/`0xF1` offsets instead. Also learned from the earlier `INFOPLIST_KEY_UIBackgroundModes` failure not to gamble on `GENERATE_INFOPLIST_FILE` supporting a nested `NSExtension` dictionary key for something this critical — used an explicit, hand-written `Info.plist` file instead (the traditional, always-correct mechanism for extension point identifiers), which needed its own `CFBundleIdentifier`/`CFBundleName`/etc. keys added by hand since `GENERATE_INFOPLIST_FILE` normally injects those automatically. Verified: full `xcodebuild build -scheme RepSetForge` succeeds with both the watch app and widget extension correctly embedded and validated (`ValidateEmbeddedBinary` passes for both); full test suite (now including 6 new `StreakServiceTests`) passes; the phone app itself still launches and runs correctly with no crash after the persistence-location change. **Scope limitation, documented**: actually seeing the rendered widget requires adding it to a home screen via a long-press-then-tap flow, which this environment has no tap-automation tool for (no iOS SpringBoard equivalent to the browser tools available) — verified everything short of that; visually confirming the widget's on-device appearance is a manual QA follow-up, the same category of gap as HealthKit's authorization prompt and Shortcuts' own app UI earlier in this list. Live Activity (rest timers) deliberately deferred to its own follow-up within this same extension target, to land as a smaller, independently-verifiable increment rather than one large combined change.)
- [x] Live Activity for active workouts and rest timers. (Landed as its own follow-up commit after Widgets above, per that item's note, to keep each increment independently verifiable. `RepSetForgeActivityAttributes` (an `ActivityAttributes` with `completedSetCount`/`totalSetCount`/`restEndDate` content state) is shared between the phone app and widget extension — the same "one file, two targets" pattern as the `@Model` classes, now applied a third time. New `LiveActivityService` (phone-only: starts/updates/ends) exposes one entry point, `startOrUpdate`, that starts a new activity if none exists or updates the existing one otherwise, so call sites never need to track activity existence themselves. Wired into the two natural trigger points: `ExerciseLoggingView`'s set-completion callback (already existed, for the local rest timer — extended to *also* update the quest-wide Live Activity, computed via `exercise.quest` since a single exercise's logging view doesn't otherwise see the whole quest's totals) and `QuestDetailView`'s `completeQuest()`/`undoCompletion()` (end the activity in both cases). The rest timer uses a native `Text(timerInterval:countsDown:)` driven by `restEndDate` rather than per-second app updates — the system ticks this itself, which is both the correct idiomatic approach and avoids a background-refresh budget problem entirely. Added `NSSupportsLiveActivities = YES` to the phone app's Info.plist settings (required for the app to be allowed to start one at all) and rendering in both the lock-screen banner and Dynamic Island (expanded + compact + minimal presentations) in the widget extension. Verified: full build succeeds (first attempt, unlike the Widgets task — applying that task's exact lessons about group-children registration and safe `u()` UUID offsets up front avoided repeating either bug); `NSSupportsLiveActivities` confirmed present via `PlistBuddy` on the built Info.plist; full test suite passes; and — the strongest verification available here — re-ran the existing `QuestLoggingUITests.testCoreQuestLoggingFlow()` UI test, which exercises the *exact* set-completion and quest-completion actions that now trigger `LiveActivityService` calls, and it passed with no crash, real confirmation the new ActivityKit calls don't destabilize the core flow even when Live Activities aren't fully authorized in this simulator environment (the `guard ActivityAuthorizationInfo().areActivitiesEnabled` check handles that gracefully). Same scope limitation as the widget itself: visually confirming the Live Activity's lock-screen/Dynamic Island appearance needs manual QA on a real device or a tap-driven simulator session this environment can't automate.)
- [x] iPad layout with denser dashboards and side-by-side quest editing. (Unlike the Watch/Widget/Live Activity items above, this needed no new Xcode target — just adaptive layout within the existing app target, so it was safe to implement directly. **Side-by-side quest editing**: rebuilt `QuestListView` around `NavigationSplitView` + `List(selection:)` instead of a plain `List` + `.navigationDestination(for:)` push stack — replaced the old `newQuest` state with a single `selectedQuest` binding shared by both the list-tap and "start new quest" flows. This is the key insight that made the change low-risk: `NavigationSplitView` adapts *automatically* — it collapses to the exact same push-navigation behavior as before on compact width (iPhone, unchanged), and shows a persistent sidebar + detail pane on regular width (iPad) — no manual size-class branching needed for this part. **Denser dashboards**: `CharacterProgressView`'s Muscle Groups and Recovery grids now read `@Environment(\.horizontalSizeClass)` and use 4 columns on regular width instead of 2, so the same information shows in half the vertical space on iPad instead of just stretching wider. No new tests — this is layout-only, no new logic to unit test. Build and all tests pass on both iPhone 17 and iPad Air 11-inch (M4) simulators. Verified live on the iPad simulator: the Character screen's 4-column grids render correctly (screenshot-checked), and the Dashboard renders correctly with the iPad-native horizontal tab bar. Couldn't screenshot the `QuestListView` split view itself — it's reached via a "View All Quests" tap from the Dashboard, which this environment can't drive — verified via the iPhone-mode build/test passing (confirming the compact collapse path still works) and code review of the split-view wiring instead.)
- [ ] Multiplayer or guild features: shared challenges, friend leaderboards, and party quests. (**Deliberately skipped, contradicts `CLAUDE.md`**: shared challenges/leaderboards/party quests need a real backend for cross-*user* state — a private per-user iCloud sync (added above) doesn't provide that; CloudKit's public/shared database types that could are a materially different, much larger undertaking of their own. Still directly contradicts this project's stated local-only-per-user architecture, not a scope/risk concern.)

## Technical Debt And Quality

- [x] Add migration tests before changing SwiftData model schemas. (There was no versioning infrastructure to test at all — `PersistenceController.schema` was a flat `Schema([...])` array with no `VersionedSchema`/`SchemaMigrationPlan`. Added `RepSetForgeSchemaV1: VersionedSchema` (the current 12-model list, now the single source of truth `PersistenceController` derives its schema from via `Schema(versionedSchema:)`) and an empty `RepSetForgeMigrationPlan: SchemaMigrationPlan` (one version, no stages yet — nothing to migrate *from* until a second version exists), with a doc comment spelling out the exact 4-step process for adding a real migration stage when the time comes. This establishes the tested *pattern* now, before it's needed under deadline pressure for an actual breaking change. `PersistenceMigrationTests` proves the pattern works: builds a real on-disk store (not in-memory) through this exact schema+migration-plan configuration, saves a character/quest/exercise/set, then reopens it via a **brand-new `ModelContainer` instance** at the same file URL — simulating an actual app relaunch rather than reusing a live context — and confirms every field and relationship survived intact; plus a guard test that `RepSetForgeSchemaV1.models` stays in sync with what's actually persisted, and a documentation-as-test check that the migration plan still has exactly one version/no stages (a deliberate tripwire: it should fail loudly the day a second version is added, prompting whoever's changed it to also add the real migration-stage test this whole item asked for). Build and all tests pass; verified the app still launches, seeds, and reaches the Dashboard correctly with the new schema-versioning wiring (screenshot-checked) — this touches `PersistenceController`, used app-wide, so this was verified with real seeded data, not just the isolated unit test.)
- [x] Expand service tests for XP recalculation, completed quest edits, streak edge cases, and achievement idempotency. (Existing coverage in `ProgressionServiceTests`/`ProgressionRebuildServiceTests`/`AchievementServiceTests` was already fairly thorough, so this was a gap-finding exercise, not a rewrite — read all three services and their existing tests first to identify what was genuinely untested rather than duplicating what already existed. Added 15 tests across the three files: **XP recalculation** — `levelUpIfNeeded`'s `while` loop cascading multiple level-ups in a single call was implemented but never exercised past one threshold (character and muscle versions both now tested crossing 3 levels at once), and `distributeXP`'s returned `DistributionResult.characterLevelUp`/`.muscleLevelUps` fields were only ever indirectly checked via the character's raw XP — added direct tests that the level-up result itself correctly reports `didLevelUp`/old/new level, including the not-leveled-up case. **Completed quest edits** — added rebuild coverage for a multi-quest replay crossing several level thresholds in one pass (not just one quest at a time, which is all the existing tests exercised), and for removing an exercise entirely from an already-completed quest (existing coverage only edited a set's reps in place, never removed a whole exercise) — confirms the removed exercise's muscle XP correctly drops to zero on rebuild rather than a stale value lingering. **Achievement idempotency** — added a test that a streak achievement's `unlockedDate` stays pinned to when it first unlocked even if `checkAchievements` is called again later with a different `at:` date, which is the actual idempotency guarantee the `unlock()` helper's `!achievement.unlocked` guard is supposed to provide but wasn't directly tested. **Streak edge cases** — added a broken-streak test: 3 consecutive days long ago, a gap, then 3 more consecutive days ending today (6 total quest-days, but no unbroken run reaches 7) — confirms `hasStreak` correctly walks back only from the most recent day rather than counting total quest-days toward a longer streak. Also filled in three achievements with zero prior test coverage at all: `ten_quests` (boundary at exactly 10 and one below), `level_10` (boundary at exactly level 10, plus confirms `first_level_up` also fires since crossing level 10 crosses level 2 too), and `hundred_sets` (exactly 100 completed sets unlocks, 99 complete + 1 incomplete does not — confirms only `completed` sets count). This is a test-only change with no production code modified, so no simulator verification was needed; build and the full suite (all 15 new tests plus every pre-existing test) pass.)
- [x] Add tests for gold/reward determinism, equipment ownership rules, and skill XP mapping once the RPG economy above is built. (The RPG economy is already built — `GoldService`, `EquipmentDropService`, `RPGEquipmentService`, `SkillProgressionService` all existed with substantial existing test suites, so this was another gap-finding pass rather than a rewrite. `GoldService` and `SkillProgressionService` were already thoroughly covered (including end-to-end gold determinism across a rebuild via `ProgressionRebuildServiceTests.testRebuildIsIdempotentForGold`), so no new tests were needed there. Added 5 tests for the two real gaps found: **equipment ownership rules** — the doc comment on `RPGEquipmentService.purchase` explicitly says a purchased item is "unequipped by default — the player still chooses when to swap it in," but nothing actually verified that claim (existing purchase tests only checked ownership and gold, never that `equippedLoadout` stayed empty for the newly-bought slot); also added a baseline test that a completely fresh context (before any starter-gear seeding or purchase) reports empty ownership/loadout rather than silently crashing on the fetch. **Reward determinism** — `EquipmentDropService`'s cross-session determinism was already tested for quest milestones but not PR milestones (added the PR-milestone equivalent), a zero-count guard was untested (`0.isMultiple(of: 3)` is `true` in Swift, so the `> 0` guard is the only thing preventing a drop at count zero — worth pinning down explicitly), and there was no test confirming quest-milestone and PR-milestone drops are computed from independent hash seeds (`"quest-3"` vs `"pr-3"`) rather than accidentally colliding or overwriting each other when both land on the same milestone number. This is a test-only change with no production code modified, so no simulator verification was needed; build and the full suite (all 5 new tests plus every pre-existing test) pass.)
- [x] Add UI tests for the core quest logging flow. (No UI test infrastructure existed at all — unlike the earlier watchOS/Widget-extension items, a UI test target is much lower risk (no entitlements, no embedding, no App Group capabilities, just a plain XCUITest bundle that depends on the app target), so this was safe to build directly rather than needing to check with the user first. Hand-wired a full `RepSetForgeUITests` target into `generate_project.py` — PBXNativeTarget with `com.apple.product-type.bundle.ui-testing`, its own file references/group/build phases/config list, a `PBXTargetDependency`+`PBXContainerItemProxy` pair on the app target (mirroring the existing unit-test target's wiring), and a `TEST_TARGET_NAME` build setting (the UI-test equivalent of the unit test target's `TEST_HOST`/`BUNDLE_LOADER`, since UI tests launch the app as a separate process and attach rather than linking into it). Also had to add the new target to the shared scheme's `.xcscheme` file directly (a `BuildActionEntry` + `TestableReference`) since that's hand-maintained separately from `generate_project.py` — building the target alone wasn't enough for `xcodebuild test` to actually run it. Added `--skip-onboarding` as a new deterministic launch argument (`PersistenceController.seedCoreDataIfNeeded()`), matching the existing `--preview-data`/`--tab` pattern, so the UI test doesn't depend on onboarding state left over from a previous run on the same simulator. Added two purely-additive `.accessibilityIdentifier`s (`currentQuestCard` on the Dashboard, `exerciseRow-<name>` on Quest Detail's skill rows) since the existing text-based labels risked being fragile to match reliably — everything else in the flow (the "Set N" completion toggle, "Complete Quest", "Done") already had a stable label/identifier from earlier accessibility work. `QuestLoggingUITests.testCoreQuestLoggingFlow()` drives the actual real flow: tap the current quest → open a skill → complete Set 1 → back → complete the quest → dismiss the completion sheet → confirm the reward shows. Iterated through two real failures found by actually running it (not just reasoning about it): the "Complete Quest" button sits in a `Form` section below the skills list and doesn't exist in the accessibility tree at all until scrolled into view (SwiftUI's lazy Form/List rendering, not just off-screen) — fixed with a bounded `swipeUp()` retry loop; and completing a quest dismisses the completion sheet back to Quest Detail, not the Dashboard as I'd assumed — fixed the final assertion to check the "Reward" section that actually appears there. Confirmed stable across two consecutive full runs (not flaky) and that the whole suite — both `RepSetForgeTests` and this new `RepSetForgeUITests` — passes together via the plain `xcodebuild test` command the project already documents, no special flags needed. Also caught and fixed two now-stale `CLAUDE.md` claims while touching the file for the new test-target documentation: "local-only, no CloudKit for MVP" (superseded when iCloud sync was added earlier) and the SwiftData model-constraints section, which now explains the actual CloudKit-driven Optional/default-value/relationship requirements instead of omitting them as N/A.)
- [x] Add snapshot or preview coverage for major SwiftUI states. (Chose preview coverage over pixel-comparison snapshot testing: every major screen already had a `#Preview`, and adding real snapshot tests would mean a first-time SPM package dependency in this hand-rolled `generate_project.py` — a materially bigger, network-dependent undertaking for comparatively little extra value here, versus the same value achievable more simply by filling the real gaps in `#Preview` coverage. Found 4 reusable components with **zero** preview coverage at all — `PerceivedEffortPicker`, `PixelAchievementCard`, `PixelQuestCard`, `QuestCompletionRewardRow` — and added one to each. Beyond just existing, "major states" (plural) was the actual ask, so also expanded two of the already-previewed top-level screens whose visual state varies most and were previously only shown in one default (empty/locked) configuration: `AchievementsView` now has three named previews (`Empty — none unlocked`, `Partially unlocked`, `All unlocked`, the last two via a small helper building an in-memory container with N achievements pre-unlocked) and `QuestHistoryView` now has two (`Empty — no completed quests`, `Populated`, the latter via a helper seeding a few completed quests with real exercises/sets). `PixelQuestCard`'s new preview covers all three quest statuses (planned/active/completed) in one view, and `PixelAchievementCard`'s covers locked vs. unlocked. Verified by building with `ENABLE_PREVIEWS = YES` (already the project's setting) — this confirms every preview's code compiles correctly, which is the actual failure mode previews protect against; rendering the Xcode canvas itself to visually confirm layout is an interactive IDE feature with no CLI equivalent, so that part is a manual-only check, same caveat as this session's other Xcode-only verification gaps. Build and full test suite pass; confirmed no regression in the actual (unmodified) view bodies via a live simulator screenshot of Achievements — only `#Preview` blocks were added, no production view code changed.)
- [x] Add a lightweight fixture factory for tests and previews. (Found the exact duplication the item describes: 10+ test files each hand-roll their own slightly-different `completedQuest(...)`/`makeCharacter(...)` helper. Added `RepSetForge/Testing/Fixtures.swift` — deliberately placed in the **app target**, not the test target, specifically so both `#Preview` blocks and `@testable import RepSetForge` test files can reach it; a copy living only in the test target would be invisible to previews. Provides `makeContainer(models:)` (in-memory, full schema by default), `makeCharacter`, `makeMuscles` (one row per `MuscleGroup`), `makeExercise` (tuple-based set convenience), `makeQuest`, and `makeCompletedQuest` — the single most duplicated exact shape (one exercise, one completed set, `daysAgo` offset) — plus `seedAchievements(context:)`. Deliberately did **not** mass-migrate the existing 10+ test files to adopt it: their local helpers differ in small, real ways (volume-based vs. reps/weight-based construction, UTC calendars for date-boundary-sensitive tests, etc.) that aren't worth the risk of reconciling into one shape for a backlog item that only asked to *add* a factory, not refactor everything onto it. Instead proved real dual-use by refactoring the two preview-container helpers written in the previous cycle's task (`AchievementsView`'s and `QuestHistoryView`'s) to build on `Fixtures` instead of hand-rolling their own `Schema`/`ModelConfiguration`/`Quest` construction — a safe refactor since I'd just written both and knew their exact behavior, and it's a real preview use, not a contrived one. Covered by a new `FixturesTests` suite (container round-trips data, `makeMuscles` covers every muscle group exactly once, `makeExercise` defaults and numbers sets correctly, `makeQuest` only sets `completedDate` when actually completed and correctly applies the `daysAgo` offset, `makeCompletedQuest`'s XP matches the real `ProgressionService` formula, `seedAchievements` inserts every definition unlocked-false). Build and full test suite pass; verified no regression via a live simulator screenshot of History (unaffected — real app data, not preview data).)
- [x] Document data model invariants, especially quest completion and XP distribution rules. (Added `Docs/DataModelInvariants.md` covering what the code depends on but states nowhere in one place: relationship/cascade rules and the CloudKit-driven Optional/default-value constraints (including *why* `Quest.exercises`/`Exercise.sets` use a private-optional-backing pattern); quest status semantics (`planned`/`active` are date-*derived*, not stored decisions — only `completed` is a real stored state, per `QuestScheduler.status(for:)`); that a quest can complete with zero sets checked (a valid outcome, not an error); and the exact XP formulas per exercise type, the 100%/40% primary/secondary muscle split (each secondary gets its own full 40%, not a shared pool), and multi-level-up cascading via the `nextLevelXP = level × 100` carry-over loop. The "rebuild from history, don't patch in place" architecture gets its own section since it's the load-bearing design decision behind undo/edit/import all being safe: exactly what gets reset to baseline vs. deleted-and-regenerated vs. preserved (skill `equipped` choice, starter/shop equipment) on every `ProgressionRebuildService.rebuild(context:)` call. Also documents gold/equipment-drop determinism (the FNV-1a `stableHash`, not `String.hashValue`, and why quest/PR milestone seeds are prefixed differently) and achievement idempotency (`unlockedDate` never overwritten by a later call). Caught and corrected one inaccuracy while writing this: my first draft claimed editing a completed quest's sets happens today and leaves `totalXP` stale — checked `ExerciseLoggingView`/`QuestDetailView` and found reps/weight render as plain read-only text once a quest is completed, so this isn't actually possible through the current UI at all; corrected the doc to state that precisely, while keeping the underlying invariant documented since it governs test code that exercises this directly (`ProgressionRebuildServiceTests.testEditingCompletedQuestSetsChangesRecalculatedXPOnRebuild`) and any future "edit completed quest" feature. Documentation-only change — no Swift code modified; build and full test suite still pass as expected.)
